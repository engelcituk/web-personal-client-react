import React from 'react'
import './CoursesList';

export default function CoursesList(props) {
    const {courses} = props;
    return (
        <div>
            courses list
        </div>
    )
}
